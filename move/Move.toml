[package]
name = "compliance_vault"
edition = "2024.beta"
license = "Apache-2.0"

[dependencies]
# Sui / MoveStdlib are added implicitly (Sui 1.45+). Do not list explicitly.

[addresses]
compliance_vault = "0x0"

[dev-addresses]
